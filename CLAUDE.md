# hazzard-reader

## Purpose
Eias's phone study site for Hazzard's Geriatric Medicine, 8e. The book is canonical; this site is for access and digestion, not a certified transcription.

## State (25 Sep 2026)
- 32 chapters live, **all lane-reviewed**. Original 6 (46, 47, 58, 60, 61, 99) earlier; the other 26 on 25 Sep (cache v21).
- **25 Sep lane review (26 parallel agents, one per chapter, brief in `tools/LANE_REVIEW_BRIEF.md`):** each chapter .md diffed both ways against the book's text layer with `tools/lane_diff.py` (MISSING / EXTRA / short SUBSTITUTIONS), disputes settled on the page image; every study bullet and drill answer re-read against the book. Source pages: per-chapter extracts in `C:\Users\eiasa\Downloads\hazzard_review\`. Found what the 23 Sep passes missed: truncated Key Clinical Points (42, 44, 59, 63, 81, 87, 105), ~20 tables still flattened to "(table text runs longer…)" run-on text — rebuilt from the page image (43-2, 44-6..9, 55-1/2/3, 57-1, 59-1/2/3/6, 87-2/3/5/7/8/10/11, 101-1/4/5/11 and others), dropped prose (42 p618/p628, 44 stimulus control, 59 p909, 63 p989, 101 p1625, 102 p1634 VCF/sacral-fracture clues), column scrambles (81, 101, 105 p1693), invented figure summaries removed (65), figures/tables mid-sentence moved (most chapters). Study-page errors fixed include ch42 MELD Q13 reversed, ch105 TST "or"→"and", ch108 Rochester RSV key wrong, ch83 CKD prevalence mislabeled as kidney failure, ch52/65/77/87 overstatements, many wrong page cites. Zero real word/number substitutions found in prose — the fast build's defects were structural, not paraphrase. Book typos kept as printed.
- `lane_diff.py` gotcha (fixed): tag-strip must be `</?[A-Za-z][^>\n]*>` — a bare `<` in prose ("< 30") used to swallow text to the next `>`. `verify_study_pages.py` now resolves the chapters dir relative to itself off-Windows (env `HAZZARD_CHAPTERS` overrides).
- **25 Sep, second pass (high-yield priority):** study pages of the 6 originally lane-cleared chapters (46, 47, 58, 60, 61, 99) had never been read against the book — reviewed (brief `tools/STUDY_REVIEW_BRIEF.md`): 88 bullet/drill fixes (e.g. ch46 silver/collagenase inhibition reversed, ch47 functional UI "GU tract normal" contradicted book, ch58 haloperidol "max 2.5 mg" invented, ch61 levodopa >70 overstated). All 40 core-nine crops viewed directly: Figure 43-3 had lost panels A/B into the 43-4 image — re-cropped both; 43-1, 44-1, 44-3 tightened; 44-4 and 44-8 top-clipped axes restored. Tables with drug/dose stacked by `<br>` lost row alignment on phone — Table 44-2 (md) and ch60's paroxetine/venlafaxine/bupropion rows (index.html template) split one drug per row. **Note: chapters 47, 58, 60, 61, 99 render from `<template>` blocks in index.html, not their .md — edit both.**
- **25 Sep, third pass:** all 199 non-core crops viewed directly (17 contact sheets). Content defects fixed: Figure 87-6 (constipation treatment algorithm) showed only its bottom third — re-cropped whole; Table 105-12 lost its 6 TB-regimen footnotes — full page re-cropped + footnotes transcribed as text; Figures 102-1 and 104-1 top-clipped — re-cropped; Table 59-7 p914 continuation was sideways — rotated upright and trimmed. 16 washed-out ghost-composited table crops (57-2, 57-3, 59-7, 77-9, 81-10, 87-9) contrast-stretched (levels 0.45–0.97, gamma 1.8). Loose crops tightened: 68-6, 81-2, 57-3 p863, 75-6. Table 87-10 (7 columns) converted to one nested list per drug.
- **25 Sep, fourth pass (independent verification):** (1) second-reviewer agents re-checked every study-page edit, every transcribed table and every crop in the core nine — first reviewers' edits all confirmed (2 minor citation/format fixes); (2) full-size crop audit of all 32 chapters found 17 incomplete crops the thumbnail pass had cleared (e.g. Figure 44-6 missing Positions 1–2, 44-7 missing the EEG/leg traces that show PLMS, 68-1 missing the whole Psych box, 42-4 missing the Aging box, 81-7 missing all footnotes, 99-8 fundus cut) — all re-cropped and viewed. **Lesson: thumbnails don't verify crops; only full-size comparison against the page does.** (3) Israeli law block audited against the statutes/circulars/procedures and the 2025 Brookdale yearbook: blockquotes that were paraphrases replaced with verbatim statute text; §17 direction fixed ("יש להימנע" — obligation, not permission); §18(ב) emergency exception, §14א specialist + director preconditions, §32ו psychiatric-consent limits (48 h) and cumulative 100,000 gift cap added (drill Q10 key was wrong); MR 20/2009 date corrected; 9 Brookdale page citations off by one fixed; all ~40 Brookdale numbers confirmed. (4) Chapter text of 46, 47, 58, 60, 61, 99 re-diffed and templates compared to .md: no defects.
- **25 Sep, fifth pass:** second-reviewer agents re-read the study pages of all 23 remaining chapters (every first-review edit + every other bullet and drill answer), the transcribed tables and a sample of chapter-text edits. First-review edits confirmed throughout; ~25 further small fixes (e.g. ch108 Rochester hospitalization 16% scoped to high-risk participants *with RSV*; ch77 SVT first-line is β-blockers/CCBs, not vagal/adenosine; ch52 PRP/MSC not an ACR recommendation; ch105 duplicate footnote block removed; ch101 book typos HLADRw52 restored as printed). Law: MR 20/2009 read in full from page images (all 20 pp) — procedure 01-09 covers only nursing/mentally-frail classification, scope qualifier added + committee/appeal/mandatory-admission bullets; Brookdale 2023-edition comparisons (rank 20, 82.3, 2021 data; 19.3%/51.9%) verified against the 2023 yearbook chapters downloaded from brookdale.jdc.org.il.
- **25 Sep, wide Codex audit (6 shards, gpt-6-astra high; cache v28):** 78 findings, each re-verified by a subagent against the page image (brief `tools/WIDE_FIX_BRIEF.md`). Study pages: dropped qualifiers/conditions restored across 25 chapters; book misprints provable from the book get `*[sic — the book's own …]*` (ch81 eosinophils cells/µL, ch98 TSH mU/L, ch98 3-signs-≥50%); book-faithful-but-outdated items keep the book text plus one `> Current practice (not the book): … — source, year` line, added only when the source was fetched and confirmed (~20 lines; 6 left unconfirmed because the guideline sites blocked fetch: pramipexole, octreotide, MRA eGFR, pacing regardless of symptoms, warfarin 5-day hold, George steroid-infection figures). Site: SW now deletes only `hazzard-*` caches (it was wiping other eiasash.github.io apps' caches), offline fallback prefers the runtime (fresher) cache, install retries each download once; search covers bullets/table cells/answers and opens the enclosing answer; md book tables no longer mislabelled "Reader summary"; `?chapter=` bad input shows the not-loaded notice; simple `**bold**` pre-converted before marked (v12 mis-paired it, 112 stray `**` on 21 pages); law drill 18 questions.
- **25 Sep, follow-up (cache v29):** the 6 pending currency items confirmed from alternative sources (Europe PMC, NCBI E-utilities, DailyMed, ACC/guideline PDFs; CHEST warfarin via Douketis 2023 slides — secondary) and current-practice lines added (ch44 pramipexole, ch45 octreotide µg, ch75 warfarin ≥5 d, ch76 MRA eGFR/K, ch77 pacing regardless of symptoms, ch101 George rates per 100 PY). Resume-card label no longer says "Chapter law"/"Chapter 45s". Law page bidi: Hebrew filenames, nevo.co.il date and "Downloads" isolated. Verifier: 52 misses (was 49) — the 3 new are false positives from a sic note / cross-chapter cite sitting between a number and its page.
- **Fixed 25 Sep (other repos):** Geriatrics #473, InternalMedicine #219, FamilyMedicine #197, ward-helper #287 (v1.46.33), Toranot #141 — each service worker (and IM/FM build.sh templates, IM/FM manual-update, Toranot reset) now deletes only its own prefixed caches; regression tests added; Codex re-review clean; merged and live. End-to-end check in a fresh browser: reader + Stage A caches survive loading all five apps.
- Leftover verifier misses (~50 across chapters) are all image-only numbers or spelled-out numbers, each checked by eye by the reviewing agent.
- A study page (summary + 15-question drill) for each of the 32 chapters, plus a Hebrew Israel-law block (`?chapter=law` / `?chapter=laws`) — primary-source Israeli law and Brookdale 65+ data (2025 edition), not from Hazzard. Law block eyebrow reads "PRIMARY LAW", not HAZZARD.
- (Was cache v20 on 23 Sep.) **The table-omission/dropped-prose/ghost-image build is CLOSED as of 23 Sep part 4 — no further scheduled sweeps.** Future defects found while studying should be fixed one at a time as encountered, not via another full-chapter sweep.
- **23 Sep, table-omission pass part 1 (32 parallel subagents, one per chapter):** compared every study page against its own table crop images (not text) for 4 defect classes — dropped tiers, merged dose/arm results, dropped exceptions/qualifiers, overstatement. 21 chapters got real fixes (see `Downloads/omission-pass-2026-09-23.md` for the full per-table log); ch83/88/98 checked clean; 8 chapters (46,47,52,58,60,61,65,99) wrongly reported as "0 table crops = nothing to check."
- **23 Sep, table-omission pass part 2 (20 parallel subagents), fixing part 1's gaps:** the "0 crop" chapters actually had their tables as inline transcribed text (not crops) — checked all 8 against that text (or the PDF where text was missing) and found real fixes in all but ch99 (checked clean-ish, see below). Re-cropped 9 broken/mislabeled table images (42-3, 43-3, 44-1 [new], 55-5, 59-7 p912+p913, 63-5 [placement only], 81-14 [was truncated before the Psychological rows], 87-4, 87-9 both pages, 65-1 [new]) and re-verified the content against each fix. Independently re-checked 4 specific edits from part 1 against the source — **all 4 confirmed correct**: ch75's bioprosthetic-vs-mechanical INR distinction, ch108's serology/RSV symbol-tier grid, ch51's alendronate eGFR (not CrCl) cutoff, and ch44's eszopiclone "avoid high-fat meal" caution (book says this verbatim). Added the 5 ch105 tables (105-1/2/3/5/6) that part 1 skipped for its line budget. Full second-pass log appended to `Downloads/omission-pass-2026-09-23.md`.
- **Root cause found for most of part 1's broken/garbled crops: a ghost-image compositing bug in the source PDF.** Several page objects (confirmed: ch59 table59-7 p912+p913, ch87 table87-9 p1373+p1374) carry TWO embedded JPEGs at the identical full-page bbox — a clean full-res scan (`Im0`) and a corrupt, half-resolution, alpha-masked duplicate (`Im1`) that composites on top and produces the overlapping-fragment garble or content-free-sliver crops seen throughout part 1. Fix: `doc.extract_image(xref)` on `Im0` alone, ignoring any half-res masked sibling at the same bbox, renders clean. **Not yet checked, flagged as likely same-bug candidates:** ch77 Table 77-9 (p1204/1205), ch57 Table 57-2/57-3, ch81 Table 81-10.
- **New defect class found in ch63: prose silently dropped by the known `format_tables()`/`handle_garbled_tables()` bug even when `<a id="pNNN">` anchors stay fully consecutive** — the 24 Sep anchor-consecutiveness sweep (below) structurally cannot catch this. ch63 lost ~900 chars around printed p993 (end of the SCA section, the "Summary of Neurodegenerative Disorders" heading + its intro paragraph, and the start of the Epilepsy section) while `p992`→`p993`→`p994` stayed consecutive; recovered from the PDF text layer and restored. **The same signature (a table's caption+image block immediately followed by the next page anchor, with no prose between) was found 8× in `chapters/hazzard8e_ch77_cardiac_arrhythmias.md` and 0× elsewhere — not verified or fixed. Check ch77 against its PDF pages before trusting it complete.**
- Other things found and left alone (flagged only, out of scope for either pass): ch60's Delirium PRN-dosing bullet drops olanzapine; ch99's DPP-4 Disadvantages bullet drops the HF-hospitalization risk; ch105's line-21 citation says (p1683) but the content is actually from Table 105-2 (p1682); ch51's INR bullets never define "risk factors" on the study page (table footnote: AF, prior thromboembolism, EF<30%, hypercoagulable state); ch42's study page says "Vulnerable Elder Survey," the book's table header says "Vulnerable Elders Survey." **23 Sep, part 3 below re-checked and mostly fixed these — see there for what actually landed.**
- **23 Sep, table-omission pass part 3 — final sweep (32 parallel subagents, one per chapter), driven by two newly-confirmed tool-bug classes.** Cache v19. Findings that change how the prior two bug classes should be understood:
  - **The "ghost-image" two-images-same-bbox signature is universal, not diagnostic.** Confirmed present on essentially every page checked across ~15 chapters (ch51, ch75, ch77, ch97, ch98, ch101, ch105 among others) — it's normal ClearScan structure (a clean full-res scan + a half-res masked duplicate), and on the large majority of pages the composite renders perfectly clean. **Only a handful of pages are actually visibly broken by it: ch57 (all 6 table crops — the worst case found), ch81 Table 81-10 (p1249-1252, confirmed real, though the existing crops were already Im0-sourced — the actual defect was un-tight full-page-dump crops with artifact panels, now fixed).** Do not treat "the pair is present" as a finding by itself — only a visibly-broken composite is.
  - **"Extract the larger image and crop from it" is not a universal fix.** On some pages (ch55 p823, ch75 p1155) the larger raster is itself a blank/highlight-overlay layer, or the ghost artifact is baked into that raster's own pixels (not a compositing defect) — the real table content is vector-drawn separately, or there is no clean alternate source at all (ch75 Table 75-6's purple title bar has an unfixable baked-in ghost; text is still fully legible, left as-is). Check the extracted image actually contains the table before trusting it.
  - **The dropped-prose detector has a false-negative mode as important as its false-positive one.** It only catches *full* paragraph deletion at a page-anchor boundary. The same underlying bug can instead **decapitate the first few words/sentence of the paragraph immediately after a table**, leaving the anchors consecutive and prose technically present-but-truncated — invisible to the anchor check AND to the caption-then-anchor heuristic. Found 3 such sites in ch76 alone (none of them flagged by either detector) by manually reading the paragraph after every table block. **This means other chapters not checked this way (most of the 32) may still have this class of gap** — the only reliable check is reading the first line after each table for a lowercase start, leading fragment, or dangling clause.
  - **A third distinct bug found: sentence/figure reordering (not deletion).** ch42 (paragraph-pair transposition), ch43 (a clause moved out of position), ch52 (a figure shoved to page-end plus its closing sentence duplicated) — no content lost, but reads wrong until fixed. Different from both the dropped-prose and ghost-image classes; same general "shared build tool mangles page-break content" family.
  - **Real prose restored this pass:** ch44 (a ~5-paragraph gap — the entire Circadian Rhythm Disorders section, general definition + all 4 disorder-specific definitions, silently deleted between p656 and Figure 44-9), ch51 (one sentence at p764, found via chapter-wide resweep, not the flagged candidate), ch76 (3 sites, ~80-450 chars each, all unflagged). Reorder-only fixes (no restoration) in ch42, ch43, ch52, ch102.
  - **Crops actually still broken despite "already fixed" status from part 1/2:** ch55 Table 55-4 (p823) was still truncated to 2 of 12 rows; ch81 Table 81-10 (all 4 pages, p1249-1252) were full-page dumps with artifact panels, never tightly cropped. Also newly found and fixed: ch57 all 6 table crops (ghost-image), ch43 Table 43-1 (truncated, missing the "External hazards" row + footnote legend), ch44 Figure 44-6 (severe under-crop) and Table 44-x_p645 (deleted — was a mislabeled duplicate of Figure 44-2, not a real table), ch58 Figure 58-1 (clipped arrow apexes), ch59 Figures 59-1 and 59-2 (both truncated — a different bug, `find_figure_region()` anchoring too low, already documented below), ch87 Figure 87-4 (truncated to a sliver), ch102 Figure 102-1 (missing its 4th pose).
  - **Study-page fixes triggered by the above:** ch44 Table 44-2's entire Opiate-agents RLS tier was missing (codeine/tramadol/oxycodone/hydrocodone/methadone) — added; ch57 Table 57-3's recognition-memory bullet was wrong because the crop that would have shown the real answer was illegible in both prior passes — corrected (AD and Wernicke-Korsakoff are the only ones with impaired recognition, not AD alone).
  - **The 5 minor gaps, resolved:** ch42 VES-13→"Vulnerable Elders Survey" fixed. ch60 olanzapine added to the PRN bullet (citation corrected to p923 only). ch99 DPP-4 HF-hospitalization risk added (deliberately left drug-class-wide, not attributed to specific agents — the book's own cell doesn't name them either). ch105's flagged citation was re-verified and found to be **already correct** — (p1683) is the more precise citation, the premise that it should say p1682 didn't survive contact with the book. ch51's flagged item was a **misattribution in the brief** — the INR/bioprosthetic-valve content named doesn't exist in ch51 (that's ch75's chapter); the agent found and fixed the real gap instead (the "risk factors" bullet for fracture risk, Table 51-4, was missing age and current smoking).
  - Full per-chapter log: `Downloads/omission-pass-2026-09-23.md` (part 3 section).
- **23 Sep, table-omission pass part 4 — FINAL sweep. Cache v20.** Two independent checks, both done to completion before closing the build:
  - **Visual re-audit of all 241 table/figure crops via 21 contact sheets (12 images each), viewed directly, not delegated.** Zero new defects found — every crop from parts 1-3 held up under direct visual inspection (including the ones "fixed twice" in part 3: ch55 Table 55-4, ch81 Table 81-10). This is the check that should have caught those two earlier — a subagent's textual claim of "fixed" is not the same as someone looking at the result. **Precision note (25 Sep, Eias's own spot-check):** "zero defects" was scoped to content-integrity — blank/truncated/garbled/rotated/mislabeled — the classes that actually lost content in parts 1-3. It did not screen for crop *tightness* (loose margins/neighbouring-column bleed) as its own class; 2 such instances turned up in a 36-crop human sample (see Cosmetic list) — content-complete, not re-audited further.
  - **Exhaustive paragraph-start check: the prose immediately after all 241 image placements, across all 32 chapters, individually verified against the PDF** (not just the anchor-heuristic's candidate sites). Found **9 more genuine restorations** beyond part 3, all missed by every prior detector:
    - **ch42, p630: ~600 words (2 full paragraphs) silently dropped**, undetected because a bold `**CHAPTER 42 FRAILTY**` running-header line sat between the figure and the next page anchor — the anchor-heuristic saw "non-empty text" there and didn't flag it. **New false-negative mode: check for a bold chapter-header line filling the gap the heuristic expects prose to fill.**
    - **ch76/ch101: column-order scrambling** (right-column text emitted before left-column text) that never disturbs page anchors — invisible to anchor-consecutiveness checks, caught only by confirming each sentence actually completes. 3 sites in ch76 (one one page beyond its own assigned scope), 1 in ch101.
    - **ch97, p1538: a new corruption class — a stitched/mashed-up sentence**, not simple deletion. Two real sentences from different parts of the page had been paraphrased together into a plausible-sounding but non-verbatim claim (a hallucination-shaped defect, not a deletion-shaped one — grammatically fluent, so nothing about it looks broken on read-through). Restored both real sentences to their correct locations. **This class is the hardest to catch — it doesn't look wrong, it just isn't what the book says. No existing detector (anchor check, dropped-prose heuristic, or a casual read) would have caught it; only a word-for-word diff against the source found it.**
    - **ch44 (reorder), ch55 (2 truncated Key Clinical Points), ch77 (figure reorder), ch88 (decapitation), ch105 (2 sites: a missing opening clause, and a sentence severed into a spurious heading)** — smaller, same general families as above.
  - **20 of 31 checked chapters (ch60 has no images, skipped) were fully clean** at every placement — parts 1-3 had already done real work.
  - **Zero study-page fixes triggered** — every restoration this pass was narrative/connective text, not a discrete testable fact.
  - Also swept and fixed in passing (found while reading, same general bug family, low-risk): ch68 had 8 instances of a spurious mid-sentence blank line at column breaks (cosmetic paragraph-splitting, no content lost) — fixed all 8 once the pattern was found, not just the one on the assigned page.
  - Full per-chapter log: `Downloads/omission-pass-2026-09-23.md` (part 4 section).
- **Known lag:** a build-agent's "done" signal (study.md appearing) can fire before it's actually finished. In the 23 Sep overnight batch this wasn't cosmetic — ch77 shipped live (v10 through v13) with 9 tables flattened into garbled text before the actual fix landed, ~40 min after the agent's own Workflow completion notification would have said so. **The Workflow tool's own completion notification is the real done-signal, not file existence** — if a script's return value hasn't arrived yet, the chapter isn't done, no matter what's on disk. After any parallel-subagent chapter build, also wait for `git status` to hold stable for ~40s before the final commit.

## Known defects in the shared build tooling
- **`build_chapter.py`: `format_tables()` runs before `handle_garbled_tables()`.** `reformat_table_body()`'s whitespace-collapse squashes a multi-row pipe table into one broken line *before* the garbled-table detector ever sees row breaks to trigger on, and a ~500-char cap then truncates mid-table, eating up to ~900 chars of adjacent prose and sometimes a `<a id="pNNN">` anchor. Hit in ch75, ch76, ch77, ch83, ch88 (not ch65) — all hand-fixed with pixel crops. **Any fast-built chapter should be grepped for non-consecutive `<a id="pNNN">` sequences** as a cheap detector for prose loss; the tool itself is not yet patched (shared file, was mid-flight across 6 concurrent agents when found). **24 Sep: swept all 32 chapters for this — only ch81 had a gap (p1248, content present, anchor marker itself missing; fixed). All 31 others are fully consecutive.**
- **`find_table_region()`/`find_figure_region()`** sometimes anchor on the wrong text match (an inline body reference to "(Table N-N)" instead of the real caption) or truncate a column-bound table/figure against an unrelated block in the other column, producing severe under-crops with no error. Also: `FIGURE_CAP_RE` requires `\d+-\d+.` and misses lettered sub-panels (77-1A, 77-4B, ...) — those need fully manual cropping. **Visually open every figure/table crop; a clean orphan-crop check only proves references resolve, not that the crop shows the right content.**
- **`tools/verify_study_pages.py`** (now committed in-repo, was scratchpad-only before 24 Sep) **— the "last bold per line" bug is fixed.** It now matches every bolded number on a line to the nearest *following* `(pNNN)` citation on that same line, not just the one immediately before the next `**`. Proven with an injected-defect fixture (`tools/test_verify_fix.py`) before trusting it: a clean two-bold-one-citation line passes, and a deliberately-wrong earlier bold is caught. Run on all 32 study pages (1628 number-claims): of 19 initial misses, **every one was a checker false positive** — the book spells the number as a word ("seven-category", "one-sixth", "zero", "the third and fourth day") rather than a digit, and the study page's digit form is a correct, reasonable restatement; NUM_RE is digit-only and can't match spelled-out numbers, so this class will keep recurring. The other 4 were real coverage gaps, now fixed: ch77's Table 77-5 (LVEF/QRS thresholds existed only inside the cropped image, never as text — added a text transcription so it's greppable); ch99's citation had extra content in the parens (`(p1578, Table 99-11)`), which `CITE_RE` doesn't match, so the claim fell through to a later, wrong citation on the same line (fixed by moving the table ref outside the parens). **24 Sep, resolved:** `CITE_RE`'s bare-parens requirement was swept across all 32 pages — 207 non-bare citations found (ranges, "(pNNN, image)", "(Table X, pNNN)" and reorderings). `CITE_GROUP_RE`/`extract_pages()` now read all of them (proven with 3 more fixture pairs). Coverage rose 1628→1751 checked claims; the 123 newly-covered claims surfaced 7 new misses, all in ch77/ch98, all confirmed correct against their source table's crop image (the checker is text-only, so an image-sourced number can never be checked by it regardless of citation parsing — that residual gap is inherent, not a bug). Zero real content errors found across either round (26 flagged claims total, 0 actual mismatches).

## Source
`C:\Users\eiasa\Downloads\hazzard marked .pdf`, 1,824 pages. PDF page = printed page + 34.

It's a phone scan (ClearScan): most of Eias's highlighting is baked into the page image (`annots=False` does not remove it), and the OCR text layer can be misaligned from the rendered pixels on rotated pages — crop from the pixels there, not the OCR boxes.

Verified printed page ranges: 42 = 615–632, 43 = 633–642, 44 = 643–664, 55 = 817–834, 59 = 893–918, 63 = 981–996, 87 = 1353–1380. Others: find by heading geometry.

## Conventions
- Tight, upright crops: artwork/table/caption/footnotes only, no neighbouring columns or figures. A note under every cropped image: *"Highlighting is the owner's annotation, not the book's."*
- `<a id="pNNN">` printed-page markers inline in the prose.
- `manifest.json` drives the chapter list and library — no HTML/JS edit needed to add a chapter or study page.
- Bump `hazzard-shell-vN` / `hazzard-runtime-vN` in `sw.js` on every deploy that changes cached content.
- Check the live URL at 390×844 before calling anything done.

## Working rules
- Fast mode: build, check once, deploy. No guards, red tests, mutation suites, cross-chapter sweeps or review rounds unless Eias asks.
- Parallel subagents (one per chapter) for multi-chapter work; the main agent checks, wires the manifest, and deploys once.
- Check every cited number against the chapter's own source text before publishing.
- Verify on the live site — never claim something is done untested; say explicitly what wasn't checked.
- Reports: 3 lines or fewer.

## Cosmetic list
- 25 Sep: ~170 crops trimmed (page tabs, page numbers, neighbour slivers, loose margins) — pure crops of existing pixels, each checked against the original. Figure 77-4D trimmed tight; Table 98-8 body-text sliver removed; Figure 44-3 re-rendered with its top frame (the "300" axis label is cut in the book itself).
- Left, unfixable by cropping: ch75 Table 75-6 ghost title bar baked into the scan; ch59 Table 59-7 p912 keeps its bottom band (it holds the rivastigmine rows p913 doesn't) and an internal dark band.
- Odd heading casing here and there.

## Site behavior (not a bug, don't chase it)
- On first load after a deploy, the previous service worker can still be controlling the page and serves the old cached manifest (no new chapter entry), so the reader falls back to its default chapter. One reload picks up the new chapter. Confirmed 22 Sep on `?chapter=law` right after the v9 deploy — resolved on second navigation with no code change. This is the SW updating on its own normal cadence, not a fetch bug; don't "fix" it by reworking the SW's fetch handler unless Eias asks.

## How it works
- `index.html` is the whole app (shell/CSS/JS from the `stage-a-reader` design). The 6 lane-reviewed chapters are embedded as `<template>` blocks.
- `manifest.json` + `chapters/*.md` is the generic path: any `?chapter=NN` with no matching template is looked up in the manifest, fetched, and rendered client-side with `marked.js` (vendored, no CDN). All 7 fast-build chapters and all 13 study pages use this path.
- `sw.js` precaches the shell plus every manifest-listed chapter's `.md` and images on install, then caches network-first / offline-fallback for everything else.
- No build step, no framework.

Live: https://eiasash.github.io/hazzard-reader/
